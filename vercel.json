{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "public"
      }
    }
  ],
  "routes": [
    { "src": "/$", "dest": "/index.html" },
    
    { "handle": "filesystem" },
    
    { "src": "/api/pois/(?<id>[^/]+)$", "dest": "/api/pois/[id].js?id=$id", "methods": ["DELETE", "GET", "POST", "PUT", "PATCH"] },

    { "src": "/([^./]+)$", "dest": "/$1/index.html" },
    { "src": "/([^./]+)/$", "dest": "/$1/index.html" }
  ]
}
